{% extends 'base.html.twig' %}

{% block title %}Edycja: {{ question.question }}{% endblock %}

{% block body %}
    <h1>Edycja: <small class="text-body-secondary">{{ question.question }}</small> </h1>

    {{ form(form) }}

    <div class="row g-1">
        <div class="col-auto">
            <a class="btn btn-outline-secondary" href="{{ path('app_questions_index') }}">powrót do listy</a>
        </div>
        <div class="col-auto">
            {{ include('questions/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
